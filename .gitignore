.venv/
__pycache__/
*.pyc
app/static/dh/

# Old local-dev schema library (before the library moved into the browser) —
# may still hold schemas you saved; import them via the Schema tab's file picker.
.local/
.env
.DS_Store


# SQLite fallback DB (used when DATABASE_URL is unset — tests + lightweight local).
.data/
*.sqlite3

# Test/tooling artifacts
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/

# Editors
.idea/
.vscode/

# Serena tool state
.serena/

# Browser Python bundle — built by scripts/build_py_bundle.py (task build:py)
app/static/py/app.zip
# Static build output — scripts/build_dist.py (task build:dist)
dist/
